 <div class="col-4 d-flex flex-column">
        <div class="card mb-4 shadow-sm d-flex flex-column">
            <div class="card-header">
                <h3 class="h4 my-0 font-weight-normal"><%=element.title %></h3>
            </div>
            <div class="card-body d-flex flex-column justify-content-between">
                <h4 class="h1 card-title pricing-card-title"><%=element.description %></h4>
                <ul class="list-unstyled mt-3 mb-4">
                    <li><%=element.author.fullname%></li>
                </ul>
                <a href="/quizz/<%=element.id%>" class="btn btn-lg btn-block btn-primary text-white">Jouer</a>
            </div>
        </div>
    </div>